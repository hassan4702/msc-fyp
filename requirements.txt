# ---- API / serving (needed to run the walking skeleton) ----
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
httpx>=0.27            # Ollama client + FastAPI TestClient

# ---- Testing ----
pytest>=8.0

# ---- Vision module (Phase 2) ----
opencv-python>=4.9      # webcam capture + face detection at inference
Pillow>=10.0            # image loading for FER training
deepface>=0.0.91
mediapipe>=0.10

# ---- Text module (Phase 3) ----
transformers>=4.40
datasets>=2.19
tokenizers>=0.19

# ---- ML core (training + evaluation, run on the M4 Max) ----
torch>=2.2
torchvision>=0.17
accelerate>=1.1.0      # required by the HF Trainer
scikit-learn>=1.4
numpy>=1.26
pandas>=2.2

# ---- Evaluation / statistics ----
scipy>=1.13            # Wilcoxon signed-rank test for the user study
matplotlib>=3.8
seaborn>=0.13
