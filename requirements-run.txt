# Minimal deps to RUN the chatbot (no training/evaluation).
# pip install -r requirements-run.txt
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
httpx>=0.27
torch>=2.2
torchvision>=0.17   # ResNet-18 backbone for the face model
transformers>=4.40
opencv-python>=4.9
mediapipe>=0.10     # BlazeFace detection; needs models/weights/mediapipe/*.tflite
Pillow>=10.0
numpy>=1.26
